{
  "name": "against-the-tribe-ecomm-store",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\" \"npm --prefix frontend-admin run dev\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
